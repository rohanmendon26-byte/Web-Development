npm i react-router-dom

What it does
Downloads the React Router DOM package from the npm registry.
Adds it to your project's node_modules folder.
Updates the package.json file by adding react-router-dom under the dependencies section.